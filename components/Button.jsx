import React from "react";

const Button = ({ styles }) => (
  
  <button type="button" className={`py-4 px-6 font-poppins font-medium text-[18px] glass rounded-[10px] outline-none ${styles}`}>
    Get Early Access
  </button>
);

export default Button;
